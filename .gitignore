# upstream clone (separate git repo, used for CRDs/reference)
agentic-controller/

node_modules/
dist/

.claude/settings.local.json
.DS_Store

# Kubeconfigs — credentials, never commit
.kube/
kubeconfig*.yaml
